rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public upload under 5MB (image/PDF)
    match /ids/{groupId}/{travelerId} {
      allow create: if request.resource.size < 5 * 1024 * 1024;
      allow read: if request.auth != null;
    }
  }
}
